apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-cicd-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/stevebowes/devops-projects.git
    targetRevision: main
    path: 02-HELM-GITOPS/helm-charts/app-chart
    helm:
      valueFiles:
        - values/cicd-prod.yaml  # ← NEW VALUES FILE
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    # Manual sync for production - requires approval
    automated: null
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
